# Please try to guess what is missing in the following code and add the missing part so that the code works fine.
# import math
# print(math.pow(2))

import math
print(math.pow(2,2))

# you simply needed to pass a second argument so that pow can calculate the power of the first argument.